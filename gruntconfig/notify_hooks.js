/* notify_hooks */
module.exports = {
  options: {
    enabled: true,
    max_jshint_notifications: 10, // maximum number of notifications from jshint output
    title: "js勉強会01" // defaults to the name in package.json, or will use project directory's name
  }
};
